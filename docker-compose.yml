version: '3.3'

services:
#   db:
#     image: mysql:5.7
#     volumes:
#       - ./db_data:/var/lib/mysql
#     restart: always
#     environment:
#       MYSQL_ROOT_PASSWORD: somewordpress
#       MYSQL_DATABASE: wordpress
#       MYSQL_USER: wordpress
#       MYSQL_PASSWORD: wordpress
   elementor:
#     depends_on:
#       - db
     build: .
     container_name: "elementor"
     ports:
       - "2222:80"
     #volumes:
     # - ./db_data:/var/lib/mysql
     restart: always
     environment:
       WORDPRESS_URL: http://localhost:2222/
#volumes:
#    db_data:
